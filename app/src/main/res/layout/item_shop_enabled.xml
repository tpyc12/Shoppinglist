<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginStart="@dimen/margin_x"
    android:layout_marginTop="@dimen/margin_half_x"
    android:layout_marginEnd="@dimen/margin_x"
    android:layout_marginBottom="@dimen/margin_half_x"
    app:cardCornerRadius="@dimen/margin_half_x"
    app:cardElevation="@dimen/margin_half_x"
    app:cardUseCompatPadding="true">

    <androidx.appcompat.widget.LinearLayoutCompat
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/background_enabled_card"
        android:orientation="horizontal"
        android:padding="@dimen/margin_xx">

        <TextView
            android:id="@+id/tv_name"
            android:layout_width="@dimen/margin_zero"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:textColor="@color/text_color"
            android:textSize="@dimen/text_size_xx"
            android:textStyle="bold"
            tools:text="Салфетки" />

        <TextView
            android:id="@+id/tv_count"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/text_color"
            android:textSize="@dimen/text_size_xx"
            android:textStyle="bold"
            tools:text="1" />

    </androidx.appcompat.widget.LinearLayoutCompat>

</androidx.cardview.widget.CardView>